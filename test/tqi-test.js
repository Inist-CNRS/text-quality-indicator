'use strict'

const pkg = require('./../package.json'),
  Tqi = require('./../src/tqi'),
  chai = require('chai'),
  expect = chai.expect;

describe(pkg.name + '/src/tqi.js', () => {
  describe('#constructor', () => {
    it('should initialize something, non ?', () => {
      // TODO : Il faut des test unitaires
    })
  })
});